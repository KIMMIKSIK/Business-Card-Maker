import React from "react";
import styles from "../../styles/login.module.css";
const Footer = React.memo(() => {
  return <footer className={styles.loginFooter}>Make your dream</footer>;
});

export default Footer;
